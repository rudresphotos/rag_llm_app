# Streamlit and dotenv
streamlit>=1.26.0
python-dotenv

# LangChain and Azure integration
langchain==0.1.13
langchain-community==0.0.29
langchain-openai==0.1.2

# RAG (Chroma)
chromadb

# Parsing documents
pypdf
python-docx

# URL scraping
beautifulsoup4
requests

# aiohttp (compatible with langchain)
aiohttp>=3.9.0
